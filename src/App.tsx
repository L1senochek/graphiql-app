import { test } from '@/utils/const/const';

const App = (): JSX.Element => {
  return (
    <>
      <div>{test}</div>
      <button>{test}</button>
      <a>{test}</a>
    </>
  );
};

export default App;
