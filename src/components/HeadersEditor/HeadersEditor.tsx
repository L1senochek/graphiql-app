import styles from './headers-editor.module.scss';

const HeadersEditor: React.FC = ({}): JSX.Element => {
  return (
    <>
      <div className={styles['headers-editor']}>HeadersEditor</div>
    </>
  );
};

export default HeadersEditor;
